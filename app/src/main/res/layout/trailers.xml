<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="150dp">

    <ImageView
        android:id="@+id/iv_trailer_preview"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="fitXY"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@mipmap/ic_launcher"
        tools:src="@mipmap/ic_launcher"/>

    <ImageView
        android:id="@+id/iv_trailer_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_play_circle_outline"
        app:layout_constraintTop_toTopOf="@+id/iv_trailer_preview"
        app:layout_constraintBottom_toBottomOf="@+id/iv_trailer_preview"
        app:layout_constraintLeft_toLeftOf="@+id/iv_trailer_preview"
        app:layout_constraintRight_toRightOf="@+id/iv_trailer_preview"/>

</android.support.constraint.ConstraintLayout>